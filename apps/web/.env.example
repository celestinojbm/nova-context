# Where the Nova API is running (server-side fetch from the Next.js app).
# The browser never calls the API directly: the web app holds the session
# token in an HttpOnly cookie and forwards it server-side (M5).
NOVA_API_URL=http://localhost:3001
