node_modules/
dist/
.next/
.output/
.wxt/
.turbo/
*.tsbuildinfo
.env
.env.local
# Filled-in production env files must NEVER be committed (they carry secrets).
# The tracked, placeholder-only `.env.production.template` is the exception.
.env.production
.env.*.local
coverage/
# Validation-gate reports (M17B) — generated, never committed.
artifacts/
.DS_Store
*.traineddata
var/
